name: Build Flutter APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar repositorio
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true

      - name: Criar base nativa Android
        run: |
          flutter create --project-name recibo_pro --platforms=android --org com.recibo.pro .

      - name: Ajustar Android compileSdk 35, Kotlin e AAPT2
        run: |
          sed -i 's/compileSdk = flutter.compileSdkVersion/compileSdk = 35/g' android/app/build.gradle || true
          sed -i 's/compileSdkVersion flutter.compileSdkVersion/compileSdkVersion 35/g' android/app/build.gradle || true
          sed -i 's/targetSdk = flutter.targetSdkVersion/targetSdk = 35/g' android/app/build.gradle || true
          sed -i 's/targetSdkVersion flutter.targetSdkVersion/targetSdkVersion 35/g' android/app/build.gradle || true
          sed -i 's/id "org.jetbrains.kotlin.android" version "[^"]*"/id "org.jetbrains.kotlin.android" version "1.9.24"/g' android/settings.gradle || true
          echo "android.aapt2Version=8.6.1-11315950" >> android/gradle.properties

      - name: Escrever pubspec.yaml
        run: |
          cat << 'EOF' > pubspec.yaml
          name: recibo_pro
          description: "Sistema Avancado de Recibos Digitais em Material 3"
          publish_to: 'none'
          version: 2.3.0+5

          environment:
            sdk: '>=3.0.0 <4.0.0'

          dependencies:
            flutter:
              sdk: flutter
            pdf: ^3.10.8
            printing: ^5.13.1
            intl: ^0.19.0
            signature: ^5.4.1
            shared_preferences: ^2.2.3
            image_picker: ^1.1.2
            url_launcher: ^6.3.0

          dev_dependencies:
            flutter_test:
              sdk: flutter
            flutter_lints: ^3.0.0

          flutter:
            uses-material-design: true
          EOF

      - name: Escrever lib/pdf_generator.dart
        run: |
          mkdir -p lib
          cat << 'EOF' > lib/pdf_generator.dart
          import 'dart:typed_data';
          import 'package:flutter/material.dart';
          import 'package:pdf/pdf.dart';
          import 'package:pdf/widgets.dart' as pw;
          import 'package:printing/printing.dart';

          class PdfGenerator {
            static Future<void> gerarECompartilhar({
              required BuildContext context,
              required String numeroRecibo,
              required String emitente,
              required String documentoEmitente,
              required String pagador,
              required String documentoPagador,
              required String valor,
              required String referente,
              required String data,
              required String operador,
              Uint8List? signatureBytes,
              Uint8List? logoBytes,
              bool apenasSegundaVia = false,
            }) async {
              final pdf = pw.Document();

              pw.Widget buildViaBlock(String tagVia, {bool isReemissao = false}) {
                return pw.Container(
                  padding: const pw.EdgeInsets.all(12),
                  decoration: pw.BoxDecoration(
                    border: pw.Border.all(color: PdfColors.blueGrey800, width: 1.2),
                    borderRadius: const pw.BorderRadius.all(pw.Radius.circular(6)),
                    color: PdfColors.white,
                  ),
                  child: pw.Column(
                    crossAxisAlignment: pw.CrossAxisAlignment.start,
                    mainAxisSize: pw.MainAxisSize.min,
                    children: [
                      pw.Row(
                        crossAxisAlignment: pw.CrossAxisAlignment.center,
                        children: [
                          if (logoBytes != null)
                            pw.Container(
                              width: 48,
                              height: 48,
                              margin: const pw.EdgeInsets.only(right: 10),
                              child: pw.Image(pw.MemoryImage(logoBytes), fit: pw.BoxFit.contain),
                            ),
                          pw.Expanded(
                            child: pw.Column(
                              crossAxisAlignment: pw.CrossAxisAlignment.start,
                              children: [
                                pw.Text(
                                  emitente.isNotEmpty ? emitente.toUpperCase() : "RECIBO DE PAGAMENTO",
                                  style: pw.TextStyle(
                                    fontSize: 13,
                                    fontWeight: pw.FontWeight.bold,
                                    color: PdfColors.blueGrey900,
                                  ),
                                ),
                                if (documentoEmitente.isNotEmpty)
                                  pw.Text("CNPJ/CPF: $documentoEmitente",
                                      style: const pw.TextStyle(fontSize: 8, color: PdfColors.grey700)),
                                pw.Row(
                                  children: [
                                    pw.Text("RECIBO Nº: $numeroRecibo",
                                        style: pw.TextStyle(fontSize: 9, fontWeight: pw.FontWeight.bold, color: PdfColors.indigo900)),
                                    pw.SizedBox(width: 8),
                                    pw.Container(
                                      padding: const pw.EdgeInsets.symmetric(horizontal: 5, vertical: 1),
                                      decoration: pw.BoxDecoration(
                                        color: isReemissao ? PdfColors.amber100 : PdfColors.blueGrey50,
                                        borderRadius: const pw.BorderRadius.all(pw.Radius.circular(3)),
                                        border: pw.Border.all(color: isReemissao ? PdfColors.amber800 : PdfColors.blueGrey300, width: 0.5),
                                      ),
                                      child: pw.Text(
                                        tagVia,
                                        style: pw.TextStyle(
                                          fontSize: 7.5,
                                          fontWeight: pw.FontWeight.bold,
                                          color: isReemissao ? PdfColors.amber900 : PdfColors.blueGrey900,
                                        ),
                                      ),
                                    ),
                                  ],
                                ),
                              ],
                            ),
                          ),
                          pw.Container(
                            padding: const pw.EdgeInsets.symmetric(horizontal: 10, vertical: 6),
                            decoration: pw.BoxDecoration(
                              color: PdfColors.grey100,
                              border: pw.Border.all(color: PdfColors.grey400, width: 0.8),
                              borderRadius: const pw.BorderRadius.all(pw.Radius.circular(4)),
                            ),
                            child: pw.Column(
                              crossAxisAlignment: pw.CrossAxisAlignment.end,
                              children: [
                                pw.Text("VALOR TOTAL", style: const pw.TextStyle(fontSize: 7, color: PdfColors.grey700)),
                                pw.Text(
                                  "R\$ $valor",
                                  style: pw.TextStyle(
                                    fontSize: 14,
                                    fontWeight: pw.FontWeight.bold,
                                    color: PdfColors.black,
                                  ),
                                ),
                              ],
                            ),
                          ),
                        ],
                      ),
                      pw.Divider(thickness: 0.6, height: 12),
                      pw.RichText(
                        text: pw.TextSpan(
                          style: const pw.TextStyle(fontSize: 9.5, lineSpacing: 1.2),
                          children: [
                            const pw.TextSpan(text: "Recebemos de "),
                            pw.TextSpan(text: pagador, style: pw.TextStyle(fontWeight: pw.FontWeight.bold)),
                            const pw.TextSpan(text: ", documento CPF/CNPJ nº "),
                            pw.TextSpan(text: documentoPagador, style: pw.TextStyle(fontWeight: pw.FontWeight.bold)),
                            const pw.TextSpan(text: ", a quantia líquida e certa de "),
                            pw.TextSpan(text: "R\$ $valor", style: pw.TextStyle(fontWeight: pw.FontWeight.bold)),
                            const pw.TextSpan(text: " correspondente ao pagamento descrito abaixo."),
                          ],
                        ),
                      ),
                      pw.SizedBox(height: 5),
                      pw.Container(
                        width: double.infinity,
                        padding: const pw.EdgeInsets.all(6),
                        decoration: pw.BoxDecoration(
                          color: PdfColors.grey50,
                          border: pw.Border.all(color: PdfColors.grey300, width: 0.5),
                          borderRadius: const pw.BorderRadius.all(pw.Radius.circular(3)),
                        ),
                        child: pw.Text(
                          "Referente: $referente",
                          style: const pw.TextStyle(fontSize: 8.5, color: PdfColors.black),
                        ),
                      ),
                      pw.SizedBox(height: 6),
                      pw.Text(
                        "Para os devidos fins de direito, firmamos e damos plena, geral e irrevogável quitação da quantia paga.",
                        style: const pw.TextStyle(fontSize: 7.5, color: PdfColors.grey700),
                      ),
                      pw.SizedBox(height: 8),
                      pw.Row(
                        mainAxisAlignment: pw.MainAxisAlignment.spaceBetween,
                        crossAxisAlignment: pw.CrossAxisAlignment.end,
                        children: [
                          pw.Column(
                            crossAxisAlignment: pw.CrossAxisAlignment.start,
                            children: [
                              pw.Text("Emissão: $data", style: const pw.TextStyle(fontSize: 8)),
                              if (operador.isNotEmpty)
                                pw.Text("Operador: $operador",
                                    style: pw.TextStyle(fontSize: 7.5, color: PdfColors.grey800, fontStyle: pw.FontStyle.italic)),
                            ],
                          ),
                          pw.Column(
                            crossAxisAlignment: pw.CrossAxisAlignment.center,
                            children: [
                              if (signatureBytes != null)
                                pw.Container(
                                  height: 32,
                                  width: 120,
                                  child: pw.Image(pw.MemoryImage(signatureBytes), fit: pw.BoxFit.contain),
                                )
                              else
                                pw.SizedBox(height: 32),
                              pw.Container(
                                width: 150,
                                decoration: const pw.BoxDecoration(
                                  border: pw.Border(bottom: pw.BorderSide(color: PdfColors.black, width: 0.7)),
                                ),
                              ),
                              pw.SizedBox(height: 2),
                              pw.Text("Assinatura do Emitente", style: const pw.TextStyle(fontSize: 7.5)),
                            ],
                          ),
                        ],
                      ),
                    ],
                  ),
                );
              }

              pdf.addPage(
                pw.Page(
                  pageFormat: PdfPageFormat.a4,
                  margin: const pw.EdgeInsets.symmetric(horizontal: 20, vertical: 16),
                  build: (pw.Context context) {
                    if (apenasSegundaVia) {
                      return pw.Column(
                        children: [
                          buildViaBlock("2ª VIA - VIA DA EMPRESA (REEMISSÃO)", isReemissao: true),
                          pw.Padding(
                            padding: const pw.EdgeInsets.symmetric(vertical: 10),
                            child: pw.Row(
                              mainAxisAlignment: pw.MainAxisAlignment.center,
                              children: [
                                pw.Text("--- 2ª VIA EMITIDA PARA COMPROVAÇÃO E ARQUIVO ---",
                                    style: const pw.TextStyle(fontSize: 8, color: PdfColors.grey600)),
                              ],
                            ),
                          ),
                          buildViaBlock("2ª VIA - VIA DO CLIENTE (REEMISSÃO)", isReemissao: true),
                        ],
                      );
                    }

                    return pw.Column(
                      children: [
                        buildViaBlock("1ª VIA - CLIENTE"),
                        pw.Padding(
                          padding: const pw.EdgeInsets.symmetric(vertical: 10),
                          child: pw.Row(
                            mainAxisAlignment: pw.MainAxisAlignment.center,
                            children: [
                              pw.Text(
                                "- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -",
                                style: const pw.TextStyle(color: PdfColors.grey500, fontSize: 6),
                              ),
                            ],
                          ),
                        ),
                        buildViaBlock("2ª VIA - EMPRESA"),
                      ],
                    );
                  },
                ),
              );

              await Printing.layoutPdf(
                onLayout: (PdfPageFormat format) async => pdf.save(),
                name: 'recibo_${numeroRecibo}_${apenasSegundaVia ? "2via" : "completo"}.pdf',
              );
            }
          }
          EOF

      - name: Escrever lib/main.dart
        run: |
          cat << 'EOF' > lib/main.dart
          import 'dart:convert';
          import 'dart:typed_data';
          import 'package:flutter/material.dart';
          import 'package:intl/intl.dart';
          import 'package:signature/signature.dart';
          import 'package:shared_preferences/shared_preferences.dart';
          import 'package:image_picker/image_picker.dart';
          import 'package:url_launcher/url_launcher.dart';
          import 'pdf_generator.dart';

          void main() => runApp(const ReciboProApp());

          class ReciboProApp extends StatelessWidget {
            const ReciboProApp({super.key});

            @override
            Widget build(BuildContext context) {
              return MaterialApp(
                title: 'Recibo Pro',
                debugShowCheckedModeBanner: false,
                theme: ThemeData(
                  useMaterial3: true,
                  colorScheme: ColorScheme.fromSeed(
                    seedColor: const Color(0xFF1E3A8A), // Azul Safira Corporativo M3
                    brightness: Brightness.light,
                  ),
                  appBarTheme: const AppBarTheme(
                    centerTitle: true,
                    elevation: 0,
                    scrolledUnderElevation: 2,
                  ),
                  cardTheme: CardTheme(
                    elevation: 0,
                    shape: RoundedRectangleBorder(
                      borderRadius: BorderRadius.circular(18),
                      side: BorderSide(color: Colors.grey.shade200, width: 1),
                    ),
                    color: Colors.white,
                  ),
                  inputDecorationTheme: InputDecorationTheme(
                    filled: true,
                    fillColor: Colors.white,
                    border: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(14),
                      borderSide: BorderSide(color: Colors.grey.shade300),
                    ),
                    enabledBorder: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(14),
                      borderSide: BorderSide(color: Colors.grey.shade300),
                    ),
                    focusedBorder: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(14),
                      borderSide: const BorderSide(color: Color(0xFF1E3A8A), width: 2),
                    ),
                    contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 16),
                  ),
                ),
                home: const HomeScreen(),
              );
            }
          }

          class HomeScreen extends StatefulWidget {
            const HomeScreen({super.key});

            @override
            State<HomeScreen> createState() => _HomeScreenState();
          }

          class _HomeScreenState extends State<HomeScreen> {
            int _currentIndex = 0;
            final _formKey = GlobalKey<FormState>();

            final _operadorController = TextEditingController();
            final _emitenteController = TextEditingController();
            final _docEmitenteController = TextEditingController();
            final _pagadorController = TextEditingController();
            final _docPagadorController = TextEditingController();
            final _valorController = TextEditingController();
            final _referenteController = TextEditingController();

            final _buscaReciboController = TextEditingController();
            final _buscaClienteController = TextEditingController();

            late SignatureController _signatureController;
            String _dataAtual = '';
            int _numeroSequencial = 1;
            Uint8List? _logoBytes;

            List<Map<String, dynamic>> _recibosSalvos = [];
            List<Map<String, dynamic>> _clientesSalvos = [];
            int? _editingReciboIndex;

            @override
            void initState() {
              super.initState();
              _dataAtual = DateFormat('dd/MM/yyyy').format(DateTime.now());
              _signatureController = SignatureController(
                penStrokeWidth: 3.2,
                penColor: const Color(0xFF1E3A8A),
                exportBackgroundColor: Colors.transparent,
              );
              _carregarConfiguracoes();
              _carregarRecibos();
              _carregarClientes();
            }

            @override
            void dispose() {
              _signatureController.dispose();
              super.dispose();
            }

            Future<void> _carregarConfiguracoes() async {
              final prefs = await SharedPreferences.getInstance();
              setState(() {
                _operadorController.text = prefs.getString('ultimo_operador') ?? '';
                _emitenteController.text = prefs.getString('empresa_nome') ?? '';
                _docEmitenteController.text = prefs.getString('empresa_doc') ?? '';
                _numeroSequencial = prefs.getInt('recibo_sequencial') ?? 1;
                final logoBase64 = prefs.getString('empresa_logo');
                if (logoBase64 != null) {
                  _logoBytes = base64Decode(logoBase64);
                }
              });
            }

            Future<void> _salvarConfiguracoes() async {
              final prefs = await SharedPreferences.getInstance();
              await prefs.setString('ultimo_operador', _operadorController.text);
              await prefs.setString('empresa_nome', _emitenteController.text);
              await prefs.setString('empresa_doc', _docEmitenteController.text);
              await prefs.setInt('recibo_sequencial', _numeroSequencial);
              if (_logoBytes != null) {
                await prefs.setString('empresa_logo', base64Encode(_logoBytes!));
              }
            }

            Future<void> _zerarNumeracao() async {
              final confirm = await showDialog<bool>(
                context: context,
                builder: (ctx) => AlertDialog(
                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(24)),
                  icon: const Icon(Icons.warning_amber_rounded, size: 36, color: Colors.amber),
                  title: const Text('Zerar Numeração?'),
                  content: const Text('O próximo recibo emitido terá o número 0001. Esta ação reinicia a sequência contábil do app.'),
                  actions: [
                    TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Cancelar')),
                    FilledButton.tonal(
                      style: FilledButton.styleFrom(backgroundColor: Theme.of(context).colorScheme.errorContainer),
                      onPressed: () => Navigator.pop(ctx, true),
                      child: Text('Zerar', style: TextStyle(color: Theme.of(context).colorScheme.error)),
                    ),
                  ],
                ),
              );

              if (confirm == true) {
                final prefs = await SharedPreferences.getInstance();
                setState(() {
                  _numeroSequencial = 1;
                });
                await prefs.setInt('recibo_sequencial', 1);
                if (!mounted) return;
                ScaffoldMessenger.of(context).showSnackBar(
                  const SnackBar(behavior: SnackBarBehavior.floating, content: Text('Numeração reiniciada para 0001!')),
                );
              }
            }

            Future<void> _selecionarLogo() async {
              final picker = ImagePicker();
              final picked = await picker.pickImage(source: ImageSource.gallery);
              if (picked != null) {
                final bytes = await picked.readAsBytes();
                setState(() {
                  _logoBytes = bytes;
                });
                _salvarConfiguracoes();
              }
            }

            Future<void> _carregarRecibos() async {
              final prefs = await SharedPreferences.getInstance();
              final raw = prefs.getString('historico_recibos');
              if (raw != null) {
                final List decoded = jsonDecode(raw);
                setState(() {
                  _recibosSalvos = decoded.cast<Map<String, dynamic>>();
                });
              }
            }

            Future<void> _carregarClientes() async {
              final prefs = await SharedPreferences.getInstance();
              final raw = prefs.getString('lista_clientes');
              if (raw != null) {
                final List decoded = jsonDecode(raw);
                setState(() {
                  _clientesSalvos = decoded.cast<Map<String, dynamic>>();
                  _ordenarClientes();
                });
              }
            }

            void _ordenarClientes() {
              _clientesSalvos.sort((a, b) => (a['nome'] as String).toLowerCase().compareTo((b['nome'] as String).toLowerCase()));
            }

            Future<void> _salvarClientes() async {
              final prefs = await SharedPreferences.getInstance();
              _ordenarClientes();
              await prefs.setString('lista_clientes', jsonEncode(_clientesSalvos));
              setState(() {});
            }

            void _abrirModalCliente([Map<String, dynamic>? clienteExistente, int? index]) {
              final nomeCtrl = TextEditingController(text: clienteExistente?['nome'] ?? '');
              final docCtrl = TextEditingController(text: clienteExistente?['documento'] ?? '');
              final telCtrl = TextEditingController(text: clienteExistente?['telefone'] ?? '');

              showModalBottomSheet(
                context: context,
                isScrollControlled: true,
                useSafeArea: true,
                showDragHandle: true,
                shape: const RoundedRectangleBorder(
                  borderRadius: BorderRadius.vertical(top: Radius.circular(28)),
                ),
                builder: (ctx) => Padding(
                  padding: EdgeInsets.only(
                    left: 20,
                    right: 20,
                    top: 10,
                    bottom: MediaQuery.of(ctx).viewInsets.bottom + 24,
                  ),
                  child: Column(
                    mainAxisSize: MainAxisSize.min,
                    crossAxisAlignment: CrossAxisAlignment.stretch,
                    children: [
                      Text(
                        clienteExistente == null ? 'Cadastrar Novo Cliente' : 'Editar Cliente',
                        style: Theme.of(ctx).textTheme.titleLarge?.copyWith(fontWeight: FontWeight.bold),
                      ),
                      const SizedBox(height: 16),
                      TextField(
                        controller: nomeCtrl,
                        decoration: const InputDecoration(labelText: 'Nome Completo', prefixIcon: Icon(Icons.person_outline)),
                      ),
                      const SizedBox(height: 12),
                      TextField(
                        controller: docCtrl,
                        decoration: const InputDecoration(labelText: 'CPF / CNPJ', prefixIcon: Icon(Icons.badge_outlined)),
                        keyboardType: TextInputType.number,
                      ),
                      const SizedBox(height: 12),
                      TextField(
                        controller: telCtrl,
                        decoration: const InputDecoration(labelText: 'Telefone / WhatsApp', prefixIcon: Icon(Icons.phone_outlined)),
                        keyboardType: TextInputType.phone,
                      ),
                      const SizedBox(height: 20),
                      FilledButton(
                        onPressed: () {
                          if (nomeCtrl.text.trim().isEmpty) return;
                          final dados = {
                            'nome': nomeCtrl.text.trim(),
                            'documento': docCtrl.text.trim(),
                            'telefone': telCtrl.text.trim(),
                          };
                          if (clienteExistente == null) {
                            _clientesSalvos.add(dados);
                          } else if (index != null) {
                            _clientesSalvos[index] = dados;
                          }
                          _salvarClientes();
                          Navigator.pop(ctx);
                        },
                        style: FilledButton.styleFrom(
                          padding: const EdgeInsets.symmetric(vertical: 16),
                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                        ),
                        child: const Text('Salvar Cliente', style: TextStyle(fontWeight: FontWeight.bold, fontSize: 16)),
                      ),
                    ],
                  ),
                ),
              );
            }

            void _preencherReciboComCliente(Map<String, dynamic> cliente) {
              setState(() {
                _pagadorController.text = cliente['nome'] ?? '';
                _docPagadorController.text = cliente['documento'] ?? '';
                _currentIndex = 0;
              });
              ScaffoldMessenger.of(context).showSnackBar(
                SnackBar(
                  behavior: SnackBarBehavior.floating,
                  content: Text("Cliente ${cliente['nome']} selecionado!"),
                ),
              );
            }

            void _carregarReciboParaEdicao(int index) {
              final r = _recibosSalvos[index];
              setState(() {
                _editingReciboIndex = index;
                _pagadorController.text = r['pagador'] ?? '';
                _docPagadorController.text = r['docPagador'] ?? '';
                _valorController.text = r['valor'] ?? '';
                _referenteController.text = r['referente'] ?? '';
                if (r['operador'] != null) _operadorController.text = r['operador'];
                _currentIndex = 0;
              });
              ScaffoldMessenger.of(context).showSnackBar(
                SnackBar(
                  behavior: SnackBarBehavior.floating,
                  content: Text("Editando Recibo Nº ${r['numero']}"),
                ),
              );
            }

            Future<void> _emitirRecibo() async {
              if (_formKey.currentState!.validate()) {
                if (_operadorController.text.trim().isEmpty) {
                  ScaffoldMessenger.of(context).showSnackBar(
                    const SnackBar(
                      behavior: SnackBarBehavior.floating,
                      content: Text('Por favor, informe o Nome do Operador!'),
                      backgroundColor: Colors.red,
                    ),
                  );
                  return;
                }

                Uint8List? signatureBytes;
                if (_signatureController.isNotEmpty) {
                  signatureBytes = await _signatureController.toPngBytes();
                }

                final numeroFormatado = _editingReciboIndex != null
                    ? _recibosSalvos[_editingReciboIndex!]['numero']
                    : _numeroSequencial.toString().padLeft(4, '0');

                final dadosRecibo = {
                  'numero': numeroFormatado,
                  'data': _dataAtual,
                  'operador': _operadorController.text.trim(),
                  'emitente': _emitenteController.text.trim(),
                  'docEmitente': _docEmitenteController.text.trim(),
                  'pagador': _pagadorController.text.trim(),
                  'docPagador': _docPagadorController.text.trim(),
                  'valor': _valorController.text.trim(),
                  'referente': _referenteController.text.trim(),
                  'assinatura': signatureBytes != null ? base64Encode(signatureBytes) : null,
                };

                final prefs = await SharedPreferences.getInstance();

                if (_editingReciboIndex != null) {
                  _recibosSalvos[_editingReciboIndex!] = dadosRecibo;
                  _editingReciboIndex = null;
                } else {
                  _recibosSalvos.insert(0, dadosRecibo);
                  _numeroSequencial++;
                  await prefs.setInt('recibo_sequencial', _numeroSequencial);
                }

                await prefs.setString('historico_recibos', jsonEncode(_recibosSalvos));
                await _salvarConfiguracoes();

                if (!mounted) return;

                await PdfGenerator.gerarECompartilhar(
                  context: context,
                  numeroRecibo: numeroFormatado,
                  emitente: _emitenteController.text.trim(),
                  documentoEmitente: _docEmitenteController.text.trim(),
                  pagador: _pagadorController.text.trim(),
                  documentoPagador: _docPagadorController.text.trim(),
                  valor: _valorController.text.trim(),
                  referente: _referenteController.text.trim(),
                  data: _dataAtual,
                  operador: _operadorController.text.trim(),
                  signatureBytes: signatureBytes,
                  logoBytes: _logoBytes,
                  apenasSegundaVia: false,
                );

                setState(() {
                  _pagadorController.clear();
                  _docPagadorController.clear();
                  _valorController.clear();
                  _referenteController.clear();
                  _signatureController.clear();
                });
              }
            }

            void _imprimirSegundaVia(Map<String, dynamic> r) {
              Uint8List? sig;
              if (r['assinatura'] != null) {
                sig = base64Decode(r['assinatura']);
              }
              PdfGenerator.gerarECompartilhar(
                context: context,
                numeroRecibo: r['numero'],
                emitente: r['emitente'] ?? _emitenteController.text,
                documentoEmitente: r['docEmitente'] ?? _docEmitenteController.text,
                pagador: r['pagador'],
                documentoPagador: r['docPagador'],
                valor: r['valor'],
                referente: r['referente'],
                data: r['data'],
                operador: r['operador'] ?? '',
                signatureBytes: sig,
                logoBytes: _logoBytes,
                apenasSegundaVia: true,
              );
            }

            void _abrirWhatsApp() async {
              final uri = Uri.parse("https://wa.me/5521974623033?text=Ol%C3%A1%20Robson%2C%20estou%20usando%20o%20Recibo%20Pro!");
              if (await canLaunchUrl(uri)) {
                await launchUrl(uri, mode: LaunchMode.externalApplication);
              }
            }

            void _abrirEmail() async {
              final uri = Uri.parse("mailto:robson.cadete@gmail.com?subject=Contato%20Recibo%20Pro");
              if (await canLaunchUrl(uri)) {
                await launchUrl(uri);
              }
            }

            Widget _buildFormAba() {
              final numExibicao = _editingReciboIndex != null
                  ? _recibosSalvos[_editingReciboIndex!]['numero']
                  : _numeroSequencial.toString().padLeft(4, '0');

              final colorScheme = Theme.of(context).colorScheme;

              return SingleChildScrollView(
                padding: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 12.0),
                child: Form(
                  key: _formKey,
                  child: Column(
                    crossAxisAlignment: CrossAxisAlignment.stretch,
                    children: [
                      Card(
                        color: colorScheme.surfaceContainerHigh,
                        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                        child: Padding(
                          padding: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 14.0),
                          child: Row(
                            mainAxisAlignment: MainAxisAlignment.spaceBetween,
                            children: [
                              Column(
                                crossAxisAlignment: CrossAxisAlignment.start,
                                children: [
                                  Text(
                                    _editingReciboIndex != null ? "EDITANDO RECIBO" : "NÚMERO DO RECIBO",
                                    style: TextStyle(fontSize: 10, fontWeight: FontWeight.w600, color: colorScheme.primary, letterSpacing: 0.5),
                                  ),
                                  Text(
                                    "Nº $numExibicao",
                                    style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold, color: colorScheme.onSurface),
                                  ),
                                ],
                              ),
                              Container(
                                padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
                                decoration: BoxDecoration(
                                  color: colorScheme.surface,
                                  borderRadius: BorderRadius.circular(10),
                                  border: Border.all(color: Colors.grey.shade300),
                                ),
                                child: Row(
                                  children: [
                                    Icon(Icons.calendar_today_outlined, size: 14, color: colorScheme.primary),
                                    const SizedBox(width: 6),
                                    Text(_dataAtual, style: const TextStyle(fontWeight: FontWeight.w500)),
                                  ],
                                ),
                              ),
                            ],
                          ),
                        ),
                      ),
                      const SizedBox(height: 14),
                      TextFormField(
                        controller: _operadorController,
                        decoration: const InputDecoration(
                          labelText: 'Nome do Operador / Emitente *',
                          prefixIcon: Icon(Icons.badge_outlined),
                          helperText: 'Obrigatório para emissão',
                        ),
                        validator: (v) => v!.trim().isEmpty ? 'Informe o nome do operador' : null,
                      ),
                      const SizedBox(height: 14),
                      TextFormField(
                        controller: _pagadorController,
                        decoration: const InputDecoration(
                          labelText: 'Nome do Cliente / Pagador',
                          prefixIcon: Icon(Icons.person_outline),
                        ),
                        validator: (v) => v!.isEmpty ? 'Informe o nome do cliente' : null,
                      ),
                      const SizedBox(height: 12),
                      TextFormField(
                        controller: _docPagadorController,
                        decoration: const InputDecoration(
                          labelText: 'CPF / CNPJ do Cliente',
                          prefixIcon: Icon(Icons.credit_card_outlined),
                        ),
                        keyboardType: TextInputType.number,
                        validator: (v) => v!.isEmpty ? 'Informe o documento' : null,
                      ),
                      const SizedBox(height: 12),
                      TextFormField(
                        controller: _valorController,
                        decoration: const InputDecoration(
                          labelText: 'Valor Total',
                          prefixText: 'R\$ ',
                          prefixIcon: Icon(Icons.attach_money),
                        ),
                        keyboardType: const TextInputType.numberWithOptions(decimal: true),
                        validator: (v) => v!.isEmpty ? 'Informe o valor' : null,
                      ),
                      const SizedBox(height: 12),
                      TextFormField(
                        controller: _referenteController,
                        maxLines: 2,
                        decoration: const InputDecoration(
                          labelText: 'Referente a...',
                          prefixIcon: Icon(Icons.description_outlined),
                        ),
                        validator: (v) => v!.isEmpty ? 'Informe a descrição' : null,
                      ),
                      const SizedBox(height: 14),
                      Row(
                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                        children: [
                          const Text("Assinatura Digital", style: TextStyle(fontSize: 14, fontWeight: FontWeight.bold)),
                          TextButton.icon(
                            onPressed: () => _signatureController.clear(),
                            icon: const Icon(Icons.clear_all, size: 18),
                            label: const Text('Limpar'),
                          ),
                        ],
                      ),
                      Container(
                        height: 125,
                        decoration: BoxDecoration(
                          color: Colors.white,
                          borderRadius: BorderRadius.circular(16),
                          border: Border.all(color: Colors.grey.shade300),
                        ),
                        child: ClipRRect(
                          borderRadius: BorderRadius.circular(16),
                          child: Signature(controller: _signatureController, height: 125, backgroundColor: Colors.white),
                        ),
                      ),
                      const SizedBox(height: 20),
                      FilledButton.icon(
                        onPressed: _emitirRecibo,
                        icon: const Icon(Icons.print_outlined),
                        label: Text(
                          _editingReciboIndex != null ? 'SALVAR E REEMITIR' : 'GERAR 1ª E 2ª VIA EM PDF',
                          style: const TextStyle(fontSize: 15, fontWeight: FontWeight.bold),
                        ),
                        style: FilledButton.styleFrom(
                          padding: const EdgeInsets.symmetric(vertical: 16),
                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
                        ),
                      ),
                    ],
                  ),
                ),
              );
            }

            Widget _buildHistoricoAba() {
              final query = _buscaReciboController.text.toLowerCase();
              final filtrados = _recibosSalvos.where((r) {
                final nome = (r['pagador'] ?? '').toString().toLowerCase();
                final num = (r['numero'] ?? '').toString().toLowerCase();
                return nome.contains(query) || num.contains(query);
              }).toList();

              final colorScheme = Theme.of(context).colorScheme;

              return Column(
                children: [
                  Padding(
                    padding: const EdgeInsets.all(14),
                    child: SearchBar(
                      controller: _buscaReciboController,
                      elevation: const WidgetStatePropertyAll(0),
                      backgroundColor: WidgetStatePropertyAll(colorScheme.surfaceContainerHigh),
                      hintText: 'Pesquisar por cliente ou nº...',
                      leading: const Icon(Icons.search),
                      trailing: [
                        if (_buscaReciboController.text.isNotEmpty)
                          IconButton(
                            icon: const Icon(Icons.clear),
                            onPressed: () {
                              _buscaReciboController.clear();
                              setState(() {});
                            },
                          ),
                      ],
                      onChanged: (_) => setState(() {}),
                    ),
                  ),
                  Expanded(
                    child: filtrados.isEmpty
                        ? Center(
                            child: Column(
                              mainAxisAlignment: MainAxisAlignment.center,
                              children: [
                                Icon(Icons.receipt_long_outlined, size: 48, color: Colors.grey.shade400),
                                const SizedBox(height: 10),
                                const Text("Nenhum recibo arquivado.", style: TextStyle(color: Colors.grey)),
                              ],
                            ),
                          )
                        : ListView.builder(
                            padding: const EdgeInsets.symmetric(horizontal: 14),
                            itemCount: filtrados.length,
                            itemBuilder: (context, index) {
                              final r = filtrados[index];
                              final realIndex = _recibosSalvos.indexOf(r);
                              return Card(
                                margin: const EdgeInsets.only(bottom: 12),
                                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
                                child: Padding(
                                  padding: const EdgeInsets.all(14),
                                  child: Column(
                                    crossAxisAlignment: CrossAxisAlignment.start,
                                    children: [
                                      Row(
                                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                                        children: [
                                          Container(
                                            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                                            decoration: BoxDecoration(
                                              color: colorScheme.primaryContainer,
                                              borderRadius: BorderRadius.circular(8),
                                            ),
                                            child: Text("Nº ${r['numero']}",
                                                style: TextStyle(fontWeight: FontWeight.bold, fontSize: 12, color: colorScheme.onPrimaryContainer)),
                                          ),
                                          Text("R\$ ${r['valor']}",
                                              style: TextStyle(fontWeight: FontWeight.bold, fontSize: 17, color: colorScheme.primary)),
                                        ],
                                      ),
                                      const SizedBox(height: 8),
                                      Text(r['pagador'], style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 15)),
                                      Text("Data: ${r['data']} • Op: ${r['operador'] ?? 'Geral'}",
                                          style: TextStyle(fontSize: 12, color: Colors.grey.shade700)),
                                      Text(r['referente'],
                                          maxLines: 1, overflow: TextOverflow.ellipsis, style: TextStyle(fontSize: 12, color: Colors.grey.shade600)),
                                      const Divider(height: 18),
                                      Row(
                                        mainAxisAlignment: MainAxisAlignment.end,
                                        children: [
                                          FilledButton.tonalIcon(
                                            style: FilledButton.styleFrom(
                                              padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
                                              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
                                            ),
                                            icon: const Icon(Icons.print_outlined, size: 16),
                                            label: const Text("2ª Via", style: TextStyle(fontSize: 12)),
                                            onPressed: () => _imprimirSegundaVia(r),
                                          ),
                                          const SizedBox(width: 8),
                                          IconButton(
                                            icon: const Icon(Icons.edit_outlined, color: Colors.amber),
                                            tooltip: 'Editar Recibo',
                                            onPressed: () => _carregarReciboParaEdicao(realIndex),
                                          ),
                                          IconButton(
                                            icon: const Icon(Icons.delete_outline, color: Colors.red),
                                            tooltip: 'Excluir',
                                            onPressed: () async {
                                              final prefs = await SharedPreferences.getInstance();
                                              _recibosSalvos.removeAt(realIndex);
                                              await prefs.setString('historico_recibos', jsonEncode(_recibosSalvos));
                                              setState(() {});
                                            },
                                          ),
                                        ],
                                      ),
                                    ],
                                  ),
                                ),
                              );
                            },
                          ),
                  ),
                ],
              );
            }

            Widget _buildClientesAba() {
              final query = _buscaClienteController.text.toLowerCase();
              final filtrados = _clientesSalvos.where((c) {
                final nome = (c['nome'] ?? '').toString().toLowerCase();
                return nome.contains(query);
              }).toList();

              final colorScheme = Theme.of(context).colorScheme;

              return Scaffold(
                backgroundColor: Colors.transparent,
                floatingActionButton: FloatingActionButton.extended(
                  onPressed: () => _abrirModalCliente(),
                  icon: const Icon(Icons.person_add_alt_1),
                  label: const Text("Novo Cliente"),
                ),
                body: Column(
                  children: [
                    Padding(
                      padding: const EdgeInsets.all(14),
                      child: SearchBar(
                        controller: _buscaClienteController,
                        elevation: const WidgetStatePropertyAll(0),
                        backgroundColor: WidgetStatePropertyAll(colorScheme.surfaceContainerHigh),
                        hintText: 'Pesquisar cliente (A-Z)...',
                        leading: const Icon(Icons.search),
                        trailing: [
                          if (_buscaClienteController.text.isNotEmpty)
                            IconButton(
                              icon: const Icon(Icons.clear),
                              onPressed: () {
                                _buscaClienteController.clear();
                                setState(() {});
                              },
                            ),
                        ],
                        onChanged: (_) => setState(() {}),
                      ),
                    ),
                    Expanded(
                      child: filtrados.isEmpty
                          ? Center(
                              child: Column(
                                mainAxisAlignment: MainAxisAlignment.center,
                                children: [
                                  Icon(Icons.people_outline, size: 48, color: Colors.grey.shade400),
                                  const SizedBox(height: 10),
                                  const Text("Nenhum cliente cadastrado.", style: TextStyle(color: Colors.grey)),
                                ],
                              ),
                            )
                          : ListView.builder(
                              padding: const EdgeInsets.symmetric(horizontal: 14),
                              itemCount: filtrados.length,
                              itemBuilder: (context, index) {
                                final c = filtrados[index];
                                final realIndex = _clientesSalvos.indexOf(c);
                                return Card(
                                  margin: const EdgeInsets.only(bottom: 10),
                                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
                                  child: ListTile(
                                    contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 6),
                                    leading: CircleAvatar(
                                      backgroundColor: colorScheme.primaryContainer,
                                      foregroundColor: colorScheme.onPrimaryContainer,
                                      child: Text(c['nome'][0].toUpperCase(), style: const TextStyle(fontWeight: FontWeight.bold)),
                                    ),
                                    title: Text(c['nome'], style: const TextStyle(fontWeight: FontWeight.bold)),
                                    subtitle: Text("Doc: ${c['documento'] ?? '-'}\nTel: ${c['telefone'] ?? '-'}"),
                                    trailing: Row(
                                      mainAxisSize: MainAxisSize.min,
                                      children: [
                                        IconButton(
                                          icon: Icon(Icons.receipt_long, color: colorScheme.primary),
                                          tooltip: 'Preencher Recibo',
                                          onPressed: () => _preencherReciboComCliente(c),
                                        ),
                                        IconButton(
                                          icon: const Icon(Icons.edit_outlined, color: Colors.grey),
                                          tooltip: 'Editar',
                                          onPressed: () => _abrirModalCliente(c, realIndex),
                                        ),
                                        IconButton(
                                          icon: const Icon(Icons.delete_outline, color: Colors.red),
                                          tooltip: 'Excluir',
                                          onPressed: () {
                                            _clientesSalvos.removeAt(realIndex);
                                            _salvarClientes();
                                          },
                                        ),
                                      ],
                                    ),
                                  ),
                                );
                              },
                            ),
                    ),
                  ],
                ),
              );
            }

            Widget _buildConfigAba() {
              final colorScheme = Theme.of(context).colorScheme;

              return SingleChildScrollView(
                padding: const EdgeInsets.all(16),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.stretch,
                  children: [
                    Card(
                      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                      child: Padding(
                        padding: const EdgeInsets.all(16),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Text("Dados da Empresa / Emitente",
                                style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold, color: colorScheme.primary)),
                            const SizedBox(height: 14),
                            TextFormField(
                              controller: _emitenteController,
                              decoration: const InputDecoration(labelText: 'Nome da Empresa / Profissional', prefixIcon: Icon(Icons.business)),
                            ),
                            const SizedBox(height: 12),
                            TextFormField(
                              controller: _docEmitenteController,
                              decoration: const InputDecoration(labelText: 'CNPJ / CPF do Emitente', prefixIcon: Icon(Icons.badge_outlined)),
                            ),
                            const SizedBox(height: 16),
                            Row(
                              children: [
                                if (_logoBytes != null)
                                  Container(
                                    width: 60,
                                    height: 60,
                                    margin: const EdgeInsets.only(right: 12),
                                    decoration: BoxDecoration(
                                      borderRadius: BorderRadius.circular(12),
                                      border: Border.all(color: Colors.grey.shade300),
                                      image: DecorationImage(image: MemoryImage(_logoBytes!), fit: BoxFit.contain),
                                    ),
                                  ),
                                Expanded(
                                  child: OutlinedButton.icon(
                                    onPressed: _selecionarLogo,
                                    icon: const Icon(Icons.image_outlined),
                                    label: Text(_logoBytes == null ? 'Adicionar Logo' : 'Alterar Logo'),
                                  ),
                                ),
                              ],
                            ),
                            const SizedBox(height: 16),
                            FilledButton(
                              onPressed: _salvarConfiguracoes,
                              style: FilledButton.styleFrom(
                                minimumSize: const Size.fromHeight(48),
                                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                              ),
                              child: const Text('Salvar Dados da Empresa'),
                            ),
                          ],
                        ),
                      ),
                    ),
                    const SizedBox(height: 14),
                    Card(
                      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                      child: Padding(
                        padding: const EdgeInsets.all(16),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Text("Controle de Numeração",
                                style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold, color: colorScheme.primary)),
                            const SizedBox(height: 8),
                            Text("Próximo número: ${_numeroSequencial.toString().padLeft(4, '0')}",
                                style: const TextStyle(fontWeight: FontWeight.w500)),
                            const SizedBox(height: 12),
                            OutlinedButton.icon(
                              onPressed: _zerarNumeracao,
                              icon: const Icon(Icons.restart_alt, color: Colors.red),
                              label: const Text('Zerar Numeração de Recibos', style: TextStyle(color: Colors.red)),
                            ),
                          ],
                        ),
                      ),
                    ),
                    const SizedBox(height: 14),
                    Card(
                      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),
                      color: colorScheme.surfaceContainerHigh,
                      child: Padding(
                        padding: const EdgeInsets.all(16),
                        child: Column(
                          crossAxisAlignment: CrossAxisAlignment.start,
                          children: [
                            Row(
                              children: [
                                Icon(Icons.code, color: colorScheme.primary),
                                const SizedBox(width: 8),
                                Text("Desenvolvedor",
                                    style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold, color: colorScheme.primary)),
                              ],
                            ),
                            const SizedBox(height: 8),
                            const Text("Robson Cadete", style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600)),
                            const SizedBox(height: 4),
                            const Text("E-mail: robson.cadete@gmail.com"),
                            const Text("Telefone: (21) 97462-3033"),
                            const SizedBox(height: 14),
                            Row(
                              children: [
                                Expanded(
                                  child: ElevatedButton.icon(
                                    onPressed: _abrirWhatsApp,
                                    icon: const Icon(Icons.chat),
                                    label: const Text("WhatsApp"),
                                    style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFF25D366), foregroundColor: Colors.white),
                                  ),
                                ),
                                const SizedBox(width: 10),
                                Expanded(
                                  child: OutlinedButton.icon(
                                    onPressed: _abrirEmail,
                                    icon: const Icon(Icons.email_outlined),
                                    label: const Text("E-mail"),
                                  ),
                                ),
                              ],
                            ),
                          ],
                        ),
                      ),
                    ),
                  ],
                ),
              );
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                appBar: AppBar(
                  title: const Text('Recibo Pro', style: TextStyle(fontWeight: FontWeight.bold)),
                ),
                body: IndexedStack(
                  index: _currentIndex,
                  children: [
                    _buildFormAba(),
                    _buildClientesAba(),
                    _buildHistoricoAba(),
                    _buildConfigAba(),
                  ],
                ),
                bottomNavigationBar: NavigationBar(
                  selectedIndex: _currentIndex,
                  onDestinationSelected: (idx) => setState(() => _currentIndex = idx),
                  destinations: const [
                    NavigationDestination(icon: Icon(Icons.receipt_long_outlined), selectedIcon: Icon(Icons.receipt_long), label: 'Emitir'),
                    NavigationDestination(icon: Icon(Icons.people_outline), selectedIcon: Icon(Icons.people), label: 'Clientes'),
                    NavigationDestination(icon: Icon(Icons.history_outlined), selectedIcon: Icon(Icons.history), label: 'Salvos'),
                    NavigationDestination(icon: Icon(Icons.settings_outlined), selectedIcon: Icon(Icons.settings), label: 'Empresa'),
                  ],
                ),
              );
            }
          }
          EOF

      - name: Baixar dependencias
        run: flutter pub get

      - name: Compilar APK do Recibo Pro
        run: flutter build apk --release --no-tree-shake-icons

      - name: Salvar APK para Download
        uses: actions/upload-artifact@v4
        with:
          name: recibo-pro-apk
          path: build/app/outputs/flutter-apk/app-release.apk
